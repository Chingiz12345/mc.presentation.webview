<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="mc.presentation.webview">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:name=".App"
            android:theme="@style/AppTheme">

        <activity
                android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name=".RetainWebViewStateActivity"
                android:configChanges="keyboard|keyboardHidden|orientation|screenSize">
        </activity>

        <activity
                android:name=".TrackPageLoadingStateActivity">
        </activity>

        <activity
                android:name=".JavaScriptInterfaceTestActivity">
        </activity>

        <activity
                android:name=".InputFileTestActivity">
        </activity>

        <activity
                android:name=".MultipleWebViewTestActivity">
        </activity>

        <activity
                android:name=".VideoPlayerTestActivity">
        </activity>

        <activity
                android:name=".TrackNetworkStateActivity">
        </activity>

        <activity
                android:name=".DataSizeTestActivity">
        </activity>

    </application>

</manifest>
